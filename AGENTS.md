# Development Guidelines

This is a standard React 19 + Vite + TypeScript application for Radiant Flower.
- Follow modern component patterns and clean architecture.
- All styles are configured via Tailwind CSS v4 in `src/styles.css`.
